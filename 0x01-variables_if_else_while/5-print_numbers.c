#include <stdio.h>
#include <stdlib.h>
/**
 * main - returns from 0-9
 *
 * Return: returns 0
 */
int main(void)
{	int num;
	for (num = 0; num <= 9; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
