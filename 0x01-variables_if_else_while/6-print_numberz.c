#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints alphabet in lowercase
 *
 * Return: always return 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		putchar(n);
	
	putchar('\n');

	return (0);

}
